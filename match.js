// ==================================================================
// © כל הזכויות שמורות | ALL RIGHTS RESERVED
// יוצר ובעלים בלעדי: דניאל אברהם חדאד | Creator & sole owner: Daniel Avraham Haddad
// אפליקציית "צאבי הצב — שומר צבי הים" וכל רכיביה הם קניינו הבלעדי. אין להעתיק/להפיץ/לשנות.
// Unauthorized copying, distribution or attribution is prohibited and actionable by law.
// ==================================================================
// 🎯 [CBY-MATCH] שכבת דירוג — איזו שאלה מתאימה לאיזה ערך.
//
// דווח: "יש עדיין המון נתקים, המנוע שלו לא משודרג".
//
// ⚠️ **מה בדיוק שבור, ומדדתי את זה ולא ניחשתי.**
//
// המנוע הקיים בוחר את **מילת המפתח הארוכה ביותר** שמופיעה
// בשאלה כמילה שלמה. הכלל הזה פשוט, מהיר, ועובד ברוב המקרים —
// אבל הוא מתעלם משאלה אחת קריטית: **איזו מילה בשאלה באמת
// מגדירה אותה.**
//
// דוגמה אמיתית מהמדידה:
//
//     שאלה:  "האם המלכה יוצאת מהכוורת"
//     תשובה: הערך "שירותים"  ❌
//
// למה? כי לערך על אזורי הכוורת יש בין מילות המפתח גם את המילה
// הגנרית "כוורת". היא באורך 5 תווים, היא מופיעה בשאלה, והיא
// ניצחה. המילה "מלכה" — שהיא הנושא האמיתי — הפסידה בתיקו
// שרירותי.
//
// ⚠️ וזו לא תקלה נקודתית. המילה "כוורת" מופיעה ב-47 ערכים
// במאגר, "דבורים" ב-113. כל שאלה שמכילה אותן חשופה לאותה
// חטיפה, ולכן זה נחווה כ"המון נתקים" ולא כבאג יחיד.
//
// **הפתרון: לשקלל לפי כמה המילה מבחינה.**
//
// מילה שמופיעה ב-113 ערכים לא מלמדת כלום — היא רקע. מילה
// שמופיעה בשלושה ערכים מצביעה ישירות על הנושא. זה בדיוק מה
// ש-IDF מודד, וזו השכבה שהייתה חסרה.
//
// ⚠️⚠️ **המנוע הישן לא נמחק ולא שונה.** השכבה הזו רצה **לפניו**
// ומחזירה תשובה רק כשהיא בטוחה — ציון מעל סף, ופער ברור מהמועמד
// השני. בכל מצב אחר היא מחזירה null, והזרימה הקיימת ממשיכה
// כרגיל. כך שיפור לא יכול לשבור מה שכבר עבד.
//
// 🔒 [CBY-PROOF] יוצר ובעלים בלעדי: דניאל אברהם חדאד / Daniel Avraham Haddad
// טביעת אצבע: CBY-T7R4L2E9 · כל העתקה ללא רשות אסורה ומקימה עילה לתביעה.
// ==================================================================

(function () {
    'use strict';
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    var OWNER = 'דניאל אברהם חדאד';
    var OWNER_EN = 'Daniel Avraham Haddad';
    var FINGERPRINT = 'CBY-T7R4L2E9';
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ------------------------------------------------------------------
    // 1. מילים חסרות תוכן
    // ------------------------------------------------------------------
    // ⚠️ מילות שאלה, גופים ופעלי פנייה. הן מופיעות בכל משפט שני
    // ולכן לא יכולות להבחין בין נושאים. "האם", "מה", "אתה",
    // "תגיד" — כולן רעש להתאמה.
    var STOP = {
        'מה':1,'מי':1,'למה':1,'איך':1,'מתי':1,'איפה':1,'כמה':1,'האם':1,'איזה':1,'איזו':1,
        'זה':1,'זו':1,'זאת':1,'הוא':1,'היא':1,'הם':1,'הן':1,'אני':1,'אתה':1,'את':1,'אתם':1,
        'אנחנו':1,'של':1,'עם':1,'על':1,'אל':1,'יש':1,'אין':1,'לא':1,'כן':1,'גם':1,'רק':1,
        'כל':1,'אבל':1,'או':1,'כי':1,'אם':1,'אז':1,'ואז':1,'נו':1,'טוב':1,
        'לי':1,'לו':1,'לה':1,'להם':1,'לנו':1,'לך':1,'אותו':1,'אותה':1,'אותי':1,'אותך':1,
        'שלי':1,'שלך':1,'שלו':1,'שלה':1,'שלנו':1,'שלכם':1,'שלהם':1,
        'היה':1,'הייתה':1,'להיות':1,'יותר':1,'הכי':1,'ממש':1,'כמו':1,'בבקשה':1,'תודה':1,
        'ספר':1,'תספר':1,'תגיד':1,'תסביר':1,'תראה':1,'רוצה':1,'אפשר':1,'בוא':1,'בואי':1,
        'קצת':1,'משהו':1,'דבר':1,'פעם':1,'עוד':1,'כבר':1,'עכשיו':1,'היום':1,
        // ⚠️⚠️ **מילות כמות — ובאג אמיתי שהן גרמו לו.**
        //
        // "ולמה כל כך הרבה?" קיבל את התשובה על "תודה". הסיבה:
        // חיתוך התחיליות הוריד את ה-ה' מ"הרבה" והפך אותה ל"רבה",
        // והמילה "רבה" קיימת באוצר המילים — כי במאגר יש מילת מפתח
        // "תודה רבה". שתי מילים שאין ביניהן שום קשר התלכדו.
        //
        // מילת כמות אינה נושא: "הרבה", "מעט", "כך" לא מבחינות בין
        // ביצים לפלסטיק, ולכן מקומן כאן ולא בחישוב.
        'הרבה':1,'מעט':1,'מאוד':1,
        // ⚠️⚠️ **מילות קישור שהפילו שאלות תמימות — וזה נמדד.**
        //
        // "למה הדבש **כזה** מתוק" לא מצאה את הערך "למה הדבש מתוק",
        // שקיים במאגר במילים כמעט זהות. המילה היחידה שהפילה אותה
        // הייתה "כזה": היא לא מופיעה באף מפתח, ולכן המנוע התייחס
        // אליה כראיה שהשאלה מחוץ לתחום — בדיוק כמו "פיצה".
        //
        // ⚠️ ניסיתי קודם לפתור את זה דרך מודל השפה (מילה שהמודל
        // מכיר = מילת קישור), וזה עבד עד שניקיתי כפילויות מהקורפוס
        // ו"כזה" נשר מאוצר המילים. הלקח: כשהתשובה ידועה מראש,
        // רשימה מפורשת עדיפה על היסק. אלה מילות קישור בעברית,
        // הן לא ישתנו, ואין סיבה ללמוד אותן מחדש בכל אימון.
        'כזה':1,'כזו':1,'כזאת':1,'כאלה':1,'כאלו':1,'ככה':1,'כך':1,
        'בכלל':1,'בעצם':1,'פשוט':1,'בערך':1,'לגמרי':1,'אולי':1,'כאילו':1
    };
    var STOP_EN = {
        'what':1,'who':1,'why':1,'how':1,'when':1,'where':1,'is':1,'are':1,'was':1,'the':1,
        'a':1,'an':1,'of':1,'to':1,'and':1,'or':1,'but':1,'do':1,'does':1,'did':1,'can':1,
        'you':1,'i':1,'it':1,'this':1,'that':1,'they':1,'them':1,'me':1,'my':1,'your':1,
        'tell':1,'about':1,'so':1,'yes':1,'no':1,'not':1,'please':1,'thanks':1,'some':1
    };
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function stopFor(lang) { return lang === 'he' ? STOP : STOP_EN; }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️ אין חיתוך תחיליות. אות מובילה בעברית היא לפעמים תחילית
    // ולפעמים חלק מהשורש — "מלכה" מול "מסירות" — וחיתוך עיוור
    // הורס בדיוק את המילים הנפוצות ביותר במאגר. במקום זה ההשוואה
    // עצמה סלחנית, וזה מטופל ב-sameWord.
    function words(text, lang) {
        var stop = stopFor(lang);
        var raw = String(text || '').toLowerCase()
            .replace(/["'`״׳()\[\]{}.,!?;:\-–—]/g, ' ')
            .split(/\s+/);
        var out = [], seen = {};
        for (var i = 0; i < raw.length; i++) {
            var w = raw[i];
            if (!w || w.length < 2 || stop[w] || seen[w]) continue;
            // ⚠️ מילת שאלה עם ו' החיבור היא עדיין מילת שאלה.
            // נמדד: "וכמה שורדים?" — "וכמה" לא הופיעה ברשימת
            // מילות העצירה, ולכן נספרה כמילה **זרה** במשקל מלא.
            // כיסוי השאלה צנח מתחת לסף, ההתאמה המצוינת לערך על
            // ההישרדות נפסלה, והילד קיבל תשובה על נושא אחר.
            if (w.length > 2 && /^[ו]/.test(w) && stop[w.slice(1)]) continue;
            seen[w] = 1;
            out.push(w);
        }
        return out;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️⚠️ **חיתוך תחילית רק כשהתוצאה היא מילה שקיימת במאגר.**
    //
    // "מהכוורת" ו"כוורת" חייבים להיראות כאותה מילה, כי זו הצורה
    // שילד כותב. אבל חיתוך עיוור של אות מובילה הורס בדיוק את
    // המילים שהאות בהן היא חלק מהשורש:
    //
    //     מסירות → סירות        מלכה → לכה
    //     כוורת  → וורת          מגנות → גנות
    //
    // כתבתי את הבאג הזה כבר פעם אחת במודול אחר, תיקנתי אותו שם,
    // וכתבתי אותו כאן שוב. הבדיקה תפסה.
    //
    // הפתרון משתמש בנתונים שכבר יש: חותכים **רק** אם הצורה
    // הקצרה היא מילה שבאמת מופיעה במאגר, ולפחות באותה שכיחות.
    // "כוורת" קיימת ב-47 ערכים ולכן "מהכוורת" נחתך; "סירות" לא
    // קיימת כלל ולכן "מסירות" נשארת שלמה.
    var vocab = null;   // נקבע בבניית האינדקס

    function tryStrip(w, cut) {
        if (w.length - cut < 3) return null;
        var short = w.slice(cut);
        if (!vocab) return null;
        // ⚠️ די בכך שהצורה הקצרה **קיימת** במאגר. הניסיון להוסיף
        // תנאי "והיא נפוצה יותר מהארוכה" נכשל מיד: "המלכה" מופיעה
        // במפתחות יותר מ"מלכה", ולכן היא לא נחתכה ושתי הצורות
        // נשארו זרות זו לזו — בדיוק הבעיה שהחיתוך נועד לפתור.
        // הבטיחות מגיעה מקיום המילה, לא מהשוואת שכיחויות.
        var dShort = vocab[short] || 0;
        if (!dShort) return null;
        return short;
    }
    // ⚠️⚠️ **חיתוך סיומת ריבוי — באותה שיטה בדיוק, ומאותה סיבה.**
    //
    // נמדד אצל צאבי: "מי מטפל בצבים **פצועים**" לא מצא כלום, בזמן
    // ש"מי מטפל בצב **פצוע**" מצא את מרכז ההצלה בציון מלא. המילה
    // "פצועים" אינה במאגר, ולכן היא נספרה כמילה **זרה** — כלומר
    // כראיה שהשאלה מחוץ לתחום — וכיסוי השאלה צנח מתחת לסף.
    //
    // ⚠️ אותה זהירות כמו בתחיליות: חותכים **רק** אם הצורה הקצרה
    // היא מילה שקיימת במאגר. בלי התנאי הזה "מים" הופך ל"מ",
    // "קיץ" ל"קי", וכל מילה שמסתיימת ב-ים או ב-ות נשברת.
    function tryStripSuffix(w) {
        if (!vocab) return null;
        var ends = ['ים', 'ות', 'יות'];
        for (var i = 0; i < ends.length; i++) {
            var e = ends[i];
            if (w.length - e.length < 3) continue;
            if (w.slice(-e.length) !== e) continue;
            var short = w.slice(0, w.length - e.length);
            if (vocab[short]) return short;
            // "פצועים" ➜ "פצוע": צורת היחיד לרוב מסתיימת ב-ה או
            // שהיא הבסיס עצמו. בודקים את שתיהן, ושתיהן חייבות
            // להתקיים במאגר.
            if (vocab[short + 'ה']) return short + 'ה';
        }
        return null;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function bare(w) {
        if (!vocab) return w;
        var base = w;
        if (/^(מה|לה|כש|וה|שה|בה)/.test(base)) {
            var two = tryStrip(base, 2);
            if (two) base = two;
        }
        if (base === w && /^[והבלמכש]/.test(base)) {
            var one = tryStrip(base, 1);
            if (one) base = one;
        }
        if (vocab[base]) return base;
        var sing = tryStripSuffix(base);
        if (sing) return sing;
        return base;
    }
    function sameWord(a, b) {
        if (a === b) return true;
        var ba = bare(a), bb = bare(b);
        return ba === bb || ba === b || bb === a;
    }
    function hasWord(list, w) {
        for (var i = 0; i < list.length; i++) if (sameWord(list[i], w)) return true;
        return false;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ------------------------------------------------------------------
    // 2. שכיחות במאגר
    // ------------------------------------------------------------------
    // ⚠️ נבנה פעם אחת ונשמר. חישוב מחדש בכל שאלה על אלף ערכים
    // מורגש כהשהיה בטלפון חלש, וזה בדיוק הקהל.
    var cache = null, cacheFor = null, cacheLen = -1;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function index(brain, lang) {
        if (cache && cacheFor === brain && cacheLen === brain.length) return cache;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        // ⚠️ מעבר ראשון — ספירת מילים **גולמית**, בלי שום חיתוך.
        // זה אוצר המילים שעליו bare מסתמך, ולכן הוא חייב להיבנות
        // לפניו. בלי ההפרדה הזו יש תלות מעגלית: חיתוך שדורש ידע
        // שנוצר רק אחרי החיתוך.
        vocab = null;
        var raw = {};
        for (var r = 0; r < brain.length; r++) {
            var er = brain[r];
            if (!er || !er.keys || er.keys[0] === 'default') continue;
            if ((er.lang || 'he') !== lang) continue;
            for (var rk = 0; rk < er.keys.length; rk++) {
                var rws = words(er.keys[rk], lang);
                for (var rw = 0; rw < rws.length; rw++) raw[rws[rw]] = (raw[rws[rw]] || 0) + 1;
            }
        }
        vocab = raw;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        var df = {}, n = 0, entries = [];
        for (var i = 0; i < brain.length; i++) {
            var e = brain[i];
            if (!e || !e.keys || e.keys[0] === 'default') continue;
            if ((e.lang || 'he') !== lang) continue;
            n++;
            var seen = {}, keyWords = [];
            for (var k = 0; k < e.keys.length; k++) {
                var kw = words(e.keys[k], lang);
                keyWords.push(kw);
                for (var j = 0; j < kw.length; j++) {
                    var b = bare(kw[j]);
                    if (!seen[b]) { seen[b] = 1; df[b] = (df[b] || 0) + 1; }
                }
            }
            // ⚠️ האם הערך עוסק בכלל בעולם הכוורת. נקבע פעם אחת
            // בבניית האינדקס, לא בכל שאלה — הבדיקה עוברת על כל
            // מילות המפתח, וזה יקר מדי לחזור עליו בזמן ריצה.
            var hasEntity = false;
            for (var he = 0; he < keyWords.length && !hasEntity; he++) {
                for (var hw = 0; hw < keyWords[he].length; hw++) {
                    if (isEntityWord(keyWords[he][hw])) { hasEntity = true; break; }
                }
            }
            entries.push({ entry: e, keyWords: keyWords, hasEntity: hasEntity });
        }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        // ⚠️⚠️ **מפתח מדויק — טבלה נפרדת, וזה תיקון של באג שנמדד.**
        //
        // הדירוג מסיר מילות עצירה לפני החישוב, ובצדק: "מה", "האם",
        // "אני", "אתה" מופיעות כמעט בכל שאלה ולא מבחינות בין נושאים.
        //
        // אבל ברגע שנוספה שכבת התקשורת הבין-אישית, ההסרה הזו הרסה
        // בדיוק את מה שהיא הייתה אמורה להגן עליו. נמדד:
        //
        //   "אתה טיפש"  ➜ אחרי הסרת מילות עצירה: ["טיפש"]
        //   "אני טיפש"  ➜ אחרי הסרת מילות עצירה: ["טיפש"]
        //
        // שתי שאלות הפוכות לגמרי — אחת עלבון ואחת ילד שמזלזל בעצמו —
        // הפכו לאותו וקטור בדיוק, והילד שכתב "אני טיפש" קיבל תשובה
        // שנכתבה למי שהעליב את צאבי. אותו דבר ב"אני לבד" מול ערך ידע
        // על כך שצבים חיים לבד.
        //
        // הפתרון אינו לבטל את מילות העצירה — הן נכונות לשאלות ידע —
        // אלא להכיר בכך שמשפט שזהה **במדויק** למילת מפתח הוא האות
        // החזק ביותר שקיים, וחזק יותר מכל שקלול. הוא נבדק לפני
        // הדירוג ומדלג עליו.
        var exact = {};
        for (var xe = 0; xe < entries.length; xe++) {
            var xk = entries[xe].entry.keys;
            for (var xj = 0; xj < xk.length; xj++) {
                var nk = normPhrase(xk[xj]);
                // ראשון זוכה: הערכים הידניים נטענים לפני המילון
                if (nk && !exact[nk]) exact[nk] = entries[xe];
            }
        }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        cache = { df: df, n: Math.max(n, 1), entries: entries, exact: exact };
        cacheFor = brain; cacheLen = brain.length;
        return cache;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    /* ——— נרמול משפט להשוואה מדויקת ——— */
    function normPhrase(s) {
        if (!s) return '';
        return String(s)
            .replace(/[֑-ׇ]/g, '')          // ניקוד
            .replace(/["'`״׳.,!?;:()\[\]{}]/g, ' ')   // פיסוק
            .replace(/[-–—]/g, ' ')                   // מקף עברי
            .replace(/\s+/g, ' ')
            .trim()
            .toLowerCase();
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️⚠️ **מילה שלא מוכרת מקבלת משקל מלא — וזה תיקון של באג
    // שהבדיקה תפסה.**
    //
    // קודם מילה לא מוכרת קיבלה משקל 0, ולכן היא נעלמה מהחישוב.
    // התוצאה: בשאלה "איך מכינים פיצה" המילה "פיצה" לא נספרה
    // בכלל, "מכינים" התאימה למפתח על הכנת דבש, וכיסוי השאלה יצא
    // **100%** — צאבי ענה על פיצה בביטחון מלא בתשובה על דבש.
    //
    // מילה שאני לא מכיר היא בדיוק ההפך מ"חסרת חשיבות": היא
    // האינדיקציה החזקה ביותר שהשאלה אינה בתחום שלי. לכן היא
    // מקבלת את המשקל הגבוה ביותר האפשרי, ומושכת את כיסוי השאלה
    // כלפי מטה עד שההתאמה נפסלת.
    // ⚠️ מילה לא מוכרת שוקלת **יותר** מכל מילה מוכרת, ולא רק
    // כמו הנדירה שבהן. בלי המכפיל נוצרה דגנרציה שהבדיקה תפסה:
    // "מכינים" מופיעה בערך אחד בלבד ולכן קיבלה משקל מרבי, בדיוק
    // כמו "פיצה" שאינה קיימת כלל. שתיהן שקלו אותו דבר, כיסוי
    // השאלה יצא 50% בדיוק, וההתאמה עברה את הסף בשערה.
    //
    // מילה שאינה במאגר היא הראיה החזקה ביותר שהשאלה מחוץ לתחום,
    // ולכן היא צריכה לשקול יותר מכל מילה שכן מוכרת לי.
    var UNKNOWN_WEIGHT = 1.6;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️⚠️ **אבל "לא מוכר" התברר כשתי מילים שונות לגמרי — וזה נמדד.**
    //
    // הכלל למעלה נכון ל"פיצה", והוא הציל את צאבי מלענות על פיצה
    // בתשובה על דבש. אבל הוא **גם פגע בשאלות תמימות לגמרי**:
    //
    //   "למה הדבש כזה מתוק"  → נדחה. הערך "למה הדבש מתוק" קיים
    //                          במאגר במילים כמעט זהות, והמילה
    //                          שהפילה אותו הייתה **"כזה"**.
    //
    // "כזה" היא לא ראיה שהשאלה מחוץ לתחום. היא מילת קישור. אבל
    // המנוע לא ידע להבחין ביניהן, כי שתיהן פשוט לא הופיעו במאגר.
    //
    // ⚠️ **וכאן מודל השפה עונה על שאלה שהמנוע לא יכול לענות
    // עליה לבדו.** המודל אומן על כל התוכן של צאבי, ולכן אוצר
    // המילים שלו הוא מפת עולם התוכן:
    //
    //     "כזה", "כך", "עושות", "מכינים"  →  מוכרות למודל
    //     "פיצה", "מחשב", "כדורגל"        →  זרות לחלוטין
    //
    // מילה שהמודל מכיר היא מילת עברית שחיה בעולם של דבורים, גם
    // אם היא לא נכתבה באף מפתח. היא מקבלת משקל בינוני. מילה
    // שאפילו המודל לא ראה מעולם — היא הזרה האמיתית, והיא מקבלת
    // את המשקל המלא.
    // ⚠️⚠️ **ושתי מילים התבררו כשלוש — וגם זה נמדד, אחרי שהגרסה
    // הקודמת של הכלל הזה הרעה תשובה אחת.**
    //
    // "מה הדבורים עושות **כשקר**" קיבלה תשובה פחות טובה, כי
    // המודל מכיר את "כשקר" (הוא מזהה בתוכו את "קר") ולכן סיווגתי
    // אותה כמילת קישור חסרת משקל. אבל "כשקר" היא **כל השאלה** —
    // בלעדיה נשאר "מה הדבורים עושות".
    //
    // ⚠️ הטעות הייתה בהנחה ש"המודל מכיר" ו"חסר חשיבות" הם אותו
    // דבר. מילה לא מוכרת למאגר יכולה להיות אחת משלוש:
    //
    //   1. **זרה לחלוטין** ("פיצה") — המודל לא ראה אותה מעולם.
    //      זו הראיה החזקה ביותר שהשאלה מחוץ לתחום. משקל מלא.
    //
    //   2. **תוכן בניסוח אחר** ("כשקר", "מתוק") — המודל מכיר
    //      אותה **וגם** מוצא לה מילה קרובה שקיימת במאגר. היא
    //      נושאת את השאלה, ולכן היא מקבלת משקל רגיל של מילה
    //      נדירה — לא עונש ולא הנחה.
    //
    //   3. **מילת קישור** ("כזה", "כך") — המודל מכיר אותה, אבל
    //      אין לה שום מילה קרובה במאגר. היא באמת לא אומרת כלום.
    //      רק היא מקבלת הנחה.
    //
    // ⚠️ התוצאה נשמרת במטמון של האינדקס: החיפוש עובר על כל אוצר
    // המילים, וילד ששואל שאלה לא צריך לחכות שזה יקרה פעמיים.
    var FILLER_WEIGHT = 0.35;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function unknownKind(w, ix) {
        if (!ix._lmKind) ix._lmKind = {};
        if (ix._lmKind[w]) return ix._lmKind[w];
        var lm = LM(), kind = 'foreign';
        if (lm && lm.has(w)) {
            kind = 'filler';
            var nb = lm.nearest(w, 12, lm.FLOOR);
            for (var i = 0; i < nb.length; i++) {
                if (ix.df[bare(nb[i].word)]) { kind = 'content'; break; }
            }
        }
        ix._lmKind[w] = kind;
        return kind;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function idf(w, ix) {
        var d = ix.df[bare(w)] || 0;
        if (!d) {
            var kind = unknownKind(w, ix);
            if (kind === 'filler')  return Math.log(ix.n + 1) * FILLER_WEIGHT;
            if (kind === 'content') return Math.log(ix.n + 1);
            return Math.log(ix.n + 1) * UNKNOWN_WEIGHT;
        }
        return Math.log((ix.n + 1) / d);
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ------------------------------------------------------------------
    // 3. הדירוג
    // ------------------------------------------------------------------
    // ⚠️ שני מדדים, ושניהם נחוצים:
    //
    //   כיסוי המפתח — כמה ממילות מילת-המפתח נענו. מפתח "מה זה
    //   מזון מלכות" שנענה רק ב"מזון" אינו התאמה.
    //
    //   כיסוי השאלה — כמה מהשאלה הוסבר, במשקל לפי מבחינות. זה
    //   מה שמונע את מקרה "שירותים": המילה "כוורת" מסבירה חלק
    //   קטן מהשאלה "האם המלכה יוצאת מהכוורת", ואילו "מלכה"
    //   מסבירה יותר.
    // ⚠️⚠️ **mustHit — התנאי שהיה חסר כשהבינה הפנימית נכנסה.**
    //
    // הבינה מוסיפה לשאלה את הנושא החסר: "וזה כואב?" הופך ל"וזה
    // כואב? דבורה". זה פותר את חוסר הנושא — ויוצר בעיה חדשה:
    // המילה "דבורה" חזקה כל כך שהערך שנקרא פשוט "דבורה" מתאים
    // לה במלואו ומנצח. הילד שאל על כאב וקיבל הגדרה של דבורה.
    //
    // התיקון: כשהנושא הושלם מבחוץ, ההתאמה חייבת לגעת גם
    // ב**מילים המקוריות** של הילד. הישות היא הקשר, לא השאלה.
    function scoreKey(qw, qIdf, qTotal, kw, ix, mustHit) {
        if (mustHit && mustHit.length) {
            var touched = false;
            for (var mh = 0; mh < kw.length; mh++) {
                if (hasWord(mustHit, kw[mh])) { touched = true; break; }
            }
            if (!touched) return null;
        }
        return scoreKeyInner(qw, qIdf, qTotal, kw, ix);
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️⚠️ **רשימת הזהויות — המילים שאסור להחליף בדומות להן.**
    //
    // אלה השמות של מי שגר בכוורת. שאלה על אחד מהם היא שאלה על
    // **הוא ולא על אחר**, ולכן שום קרבה סמנטית לא מספיקה כאן.
    // זה הכלל שמונע את החזרת הבאג שדווח פעמיים: "שאלתי לגבי
    // הפועלת אם היא יכולה למות והוא ענה את אותה התשובה שענה על
    // המלכה".
    //
    // ⚠️ הרשימה כוללת גם צורות עם ה' הידיעה, כי הן מילים נפרדות
    // לגמרי מבחינת ההתאמה.
    var ENTITY_WORDS = {};
    ('צב הצב צבים הצבים צאבי אבקוע האבקוע אבקועים האבקועים ' +
     'ביצה הביצה ביצים הביצים קן הקן קינים הקינים ' +
     'שריון השריון סנפיר הסנפיר סנפירים נקבה הנקבה נקבות זכר הזכר זכרים ' +
     'ים הים חוף החוף חופים חול החול ' +
     'פלסטיק הפלסטיק שקית שקיות ניילון רשת הרשת רשתות דייג ' +
     'שועל השועל שועלים תן תנים תאורה אור אורות ' +
     'מתנדב מתנדבים פקח פקחים מכמורת הדגרה ' +
     'מדוזה מדוזות אצות לוגרהד גלדי ניצי')
        .split(' ').forEach(function (w) { ENTITY_WORDS[w] = 1; });
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function isEntityWord(w) { return ENTITY_WORDS[w] === 1; }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️ המודל הפנימי, אם נטען. אם לא — הכול עובד בדיוק כמו קודם.
    function LM() {
        var g = typeof window !== 'undefined' ? window
              : (typeof self !== 'undefined' ? self : global);
        var m = g && g.CBY_LM;
        return (m && m.isReady()) ? m : null;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    function scoreKeyInner(qw, qIdf, qTotal, kw, ix) {
        if (!kw.length) return null;
        var lm = LM();
        // ⚠️⚠️ **כאן המודל הפנימי נכנס למנוע — וכאן צריך זהירות.**
        //
        // דווח: הילד שאל "וזה כואב?" והמנוע הגיע לערך "מדהים".
        // המילה "כואב" לא כתובה באף מפתח, ולכן התאמת מחרוזות לא
        // יכלה למצוא את הערך על העוקץ — לא משנה כמה ערכים נוסיף.
        //
        // עכשיו, כשמילת מפתח לא מופיעה בשאלה מילולית, המודל נשאל
        // אם יש בשאלה מילה שמדברת על אותו דבר. אבל **הזיכוי הוא
        // חלקי בלבד** (מוכפל בקרבה, שלעולם קטנה מ-1, וב-0.6
        // נוספים בתוך המודל). הסיבה: קרבה סמנטית היא רמז, לא ראיה.
        // אילו הייתה שווה בערכה להתאמה מפורשת, כל מילה מעורפלת
        // הייתה מושכת עשרה ערכים והמנוע היה נעשה **פחות** מדויק.
        var hit = 0, soft = 0, gained = 0;
        for (var i = 0; i < kw.length; i++) {
            if (hasWord(qw, kw[i])) {
                hit++;
                gained += idf(kw[i], ix);
            } else if (lm) {
                var aff = lm.affinity(kw[i], qw);
                if (aff > 0) { soft += aff; gained += idf(kw[i], ix) * aff; }
            }
        }
        // ⚠️ **חייבת להיות לפחות התאמה מילולית אחת.** ערך שנבחר
        // כולו על סמך קרבה סמנטית הוא ניחוש מנוסח יפה, וזו בדיוק
        // ההתנהגות שהמשתמש תיאר כ"הוא מדבר על משהו אחר לגמרי".
        if (!hit) return null;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        // ⚠️⚠️ **הנושא של המפתח חייב להופיע בשאלה.**
        //
        // דווח: "שאלתי לגבי הפועלת אם היא יכולה למות והוא ענה את
        // אותה התשובה שענה על המלכה".
        //
        // המפתח "המלכה יכולה למות" התאים לשאלה "הפועלת יכולה
        // למות" בשתי מילים מתוך שלוש — 67% כיסוי, מעל הסף. המילה
        // היחידה שלא התאימה הייתה **"המלכה"**, כלומר בדיוק הנושא.
        //
        // וזה הכלל שהיה חסר: לא כל המילים במפתח שוות. המילה
        // המבחינה ביותר בו היא הנושא שלו, ואם היא לא מופיעה
        // בשאלה — השאלה על משהו אחר, לא משנה כמה מילים אחרות
        // התאימו. "יכולה" ו"למות" מתארות פעולה, לא נושא.
        // ⚠️⚠️ **תיקו בין מילים חייב להישבר לפי השאלה, לא לפי הסדר.**
        //
        // "איך נולדת דבורה" לא מצאה את הערך "כמה ימים עד שדבורה
        // **נולדת**". הסיבה: "ימים" ו"נולדת" מופיעות שתיהן בשני
        // ערכים בלבד, כלומר ערך ההבחנה שלהן **זהה בדיוק**. הקוד
        // בחר את הראשונה ברשימה — "ימים" — הכריז שהיא הנושא,
        // וראה שהיא לא בשאלה. הערך נדחה בגלל סדר המילים במפתח.
        //
        // הכלל נוסח כדי לוודא ש**הנושא** של המפתח נמצא בשאלה.
        // כששתי מילים מועמדות באותה מידה לתפקיד הנושא, ואחת מהן
        // כן מופיעה — המפתח בנושא הנכון. מה שנדרש הוא שאחת מהן
        // תיענה, ולא שהראשונה מביניהן תיענה.
        var headIdf = -1;
        for (var h0 = 0; h0 < kw.length; h0++) {
            var v0 = idf(kw[h0], ix);
            if (v0 > headIdf) headIdf = v0;
        }
        var heads = [], headHit = false;
        for (var h = 0; h < kw.length; h++) {
            if (idf(kw[h], ix) < headIdf - 1e-9) continue;
            heads.push(kw[h]);
            if (hasWord(qw, kw[h])) headHit = true;
        }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        // ⚠️⚠️ **וכאן ההבחנה שהייתה חסרה — והיא נמדדה.**
        //
        // הכלל "הנושא חייב להופיע מילולית" נוסח כדי למנוע את הבאג
        // שדווח פעמיים (תשובה על המלכה לשאלה על הפועלת). הוא אכן
        // מנע אותו — אבל הוא גם **חסם את מודל השפה בדיוק במקרים
        // שבשבילם הוא נבנה**:
        //
        //   "עם מה דבורה עפה"  → הערך על הכנפיים נדחה כי "כנפיים"
        //                        לא הופיע מילולית.
        //   "איך נולדת דבורה"  → הערך על הביצה והזחל נדחה.
        //
        // ההבחנה הנכונה היא לא בין מילולי לסמנטי, אלא בין **שם של
        // מישהו לבין תיאור של משהו**:
        //
        //   "מלכה" ו"פועלת" הן **זהויות**. דמיון אינו זהות — שתי
        //   דבורות שונות מתוארות באותן מילים בדיוק. לכן שם של
        //   ישות חייב להופיע מילולית, תמיד.
        //
        //   "כנפיים" ו"עפה" הן **תיאורים**. ילד שאומר "עם מה היא
        //   טסה" מתכוון בדיוק לאותו דבר, וכאן המודל צודק.
        //
        // ⚠️ הסף כאן (0.85) גבוה מסף הרמז הרגיל. הנושא הוא ההחלטה
        // הכי כבדה בהתאמה, ולכן הוא דורש את הביטחון הכי גבוה.
        // ⚠️⚠️ **ומילה אחת בשאלה לא יכולה למלא שני תפקידים.**
        //
        // "מה זה כוורת" קיבלה תשובה על רישיון לגידול דבורים
        // במרפסת. המפתח היה "כוורת בבית": המילה "כוורת" בשאלה
        // התאימה מילולית ל"כוורת" במפתח, **ואז אותה מילה עצמה**
        // שימשה שוב כדי לספק את הנושא "בבית" בקרבה סמנטית.
        //
        // מילה אחת ספרה פעמיים, והמפתח נראה כאילו נענה במלואו.
        // לכן החלפה סמנטית של הנושא מותרת רק למילה בשאלה
        // ש**עדיין לא נוצלה** להתאמה מילולית.
        if (kw.length > 1 && !headHit && lm) {
            for (var hh = 0; hh < heads.length && !headHit; hh++) {
                if (isEntityWord(heads[hh]) || isEntityWord(bare(heads[hh]))) continue;
                // ⚠️⚠️ **ההחלטה על הנושא דורשת שהמודל יהיה בטוח.**
                //
                // "איפה הדבורים גרות" קיבל תשובה על שנת 1852, כי
                // המודל קישר את "גרות" ל"הומצאה" בביטחון 0.97 —
                // על סמך משפט אחד בקורפוס. ציון גבוה על ראיה
                // דלה הוא בדיוק המצב המסוכן: הוא **נשמע** בטוח.
                //
                // לכן להחלפת הנושא נדרשות שתי בדיקות נפרדות: שהקרבה
                // גבוהה, ושהמודל ראה את שתי המילים במספיק הקשרים
                // שונים כדי שהקרבה תהיה שווה משהו.
                if (!lm.confident(heads[hh])) continue;
                for (var q = 0; q < qw.length; q++) {
                    if (hasWord(kw, qw[q])) continue;   // כבר משמשת במקום אחר
                    if (!lm.confident(qw[q])) continue;
                    if (lm.sim(heads[hh], qw[q]) >= 0.85) { headHit = true; break; }
                }
            }
        }
        // ⚠️⚠️ **והכלל הזה נשאר מילולי לחלוטין — בכוונה מלאה.**
        //
        // מפתה לתת למודל הפנימי לרכך גם כאן, אבל זו הייתה טעות
        // חמורה: "מלכה" ו"פועלת" קרובות מאוד במודל, כי שתיהן
        // מופיעות באותם הקשרים בדיוק. כלומר קרבה סמנטית על הנושא
        // הייתה **מחזירה בדיוק את הבאג שדווח פעמיים** — התשובה על
        // המלכה לשאלה על הפועלת.
        //
        // קרבה סמנטית טובה לזיהוי *על מה מדברים*. היא רעה לזיהוי
        // *על מי מדברים*, כי היא לא מבחינה בין דומים לזהים.
        if (kw.length > 1 && !headHit) return null;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        var keyCov = (hit + soft) / kw.length;
        var qCov = qTotal > 0 ? gained / qTotal : 0;
        // ⚠️ מפתח בן מילה אחת חייב להיענות במלואו. בלי זה מפתח
        // "דבורים" היה נחשב התאמה לכל שאלה במאגר.
        if (kw.length === 1 && keyCov < 1) return null;
        // ⚠️ מפתח ארוך שנענה חלקית הוא לרוב מקרי.
        if (kw.length >= 3 && keyCov < 0.6) return null;
        return {
            score: gained * (0.55 + 0.45 * keyCov) * (0.55 + 0.45 * qCov),
            keyCov: keyCov, qCov: qCov, hit: hit, len: kw.length
        };
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ⚠️ ספי הביטחון. הם מכוילים כך שהשכבה מדברת רק כשהיא באמת
    // יודעת, ושותקת בכל מצב אחר — כי המנוע הישן עדיין שם ועובד.
    var MIN_SCORE = 1.6;   // מתחת לזה — מילה שכיחה מדי
    // ⚠️ הועלה מ-0.30 ל-0.48 אחרי שהבדיקה תפסה את "איך מכינים פיצה"
// → "דבש". המילה "מכינים" לבדה הסבירה 40% מהשאלה וזה הספיק.
// חצי מהשאלה הוא הרף שמפריד בין "הבנתי על מה מדובר" לבין
// "תפסתי מילה אחת ורצתי איתה".
var MIN_QCOV = 0.48;
    var MARGIN = 1.12;     // פער נדרש מהמועמד השני

    function best(text, brain, lang, opts) {
        lang = lang || 'he';
        var mustHit = (opts && opts.mustHit) ? words(opts.mustHit, lang) : null;
        var ix = index(brain, lang);
        // ⚠️ מפתח מדויק גובר על הדירוג. ההסבר המלא בבניית האינדקס.
        // נבדק על מה ש**הילד** כתב (mustHit), לא על ההשלמה של הבינה —
        // אחרת "וזה מסוכן?" שהושלם ל"וזה מסוכן? פלסטיק" לא היה מזוהה.
        var rawAsked = (opts && opts.mustHit) ? opts.mustHit : text;
        var exactHit = ix.exact && ix.exact[normPhrase(rawAsked)];
        if (exactHit) {
            return {
                entry: exactHit.entry,
                score: 99,
                qCov: 1,
                kCov: 1,
                ambiguous: false,
                exact: true
            };
        }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        var qw = words(text, lang);
        if (!qw.length) return null;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        var qIdf = [], qTotal = 0;
        for (var i = 0; i < qw.length; i++) {
            var v = idf(qw[i], ix);
            qIdf.push(v); qTotal += v;
        }
        if (qTotal <= 0) return null;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        // ⚠️ האם הילד נקב בשם של מישהו מהכוורת. ראה ההסבר המלא
        // בלולאה למטה.
        // ⚠️⚠️ **נבדק על מה ש**הילד** אמר, לא על מה שהבינה השלימה.**
        //
        // נמדד: "למה זה כואב כל כך" הפסיק לעבוד אחרי שהוספתי את
        // הכלל הזה. הסיבה: הבינה משלימה נושא חסר, ולכן המשפט
        // שהגיע לכאן היה "למה זה כואב כל כך **דבורה**". המילה
        // "דבורה" הפעילה את הסינון, והערך על כאב העקיצה — שמילות
        // המפתח שלו הן "כמה זה כואב", בלי שום שם של ישות — נפסל.
        //
        // כלומר כלל שנועד לכבד את מה שהילד ביקש נשבר בדיוק כי
        // הוא הסתכל על מה שאנחנו הוספנו. mustHit מחזיק את הטקסט
        // המקורי, וזה מה שצריך להיבדק.
        var askedWords = mustHit || qw;
        var askedEntity = false;
        for (var ae = 0; ae < askedWords.length; ae++) {
            if (isEntityWord(askedWords[ae]) || isEntityWord(bare(askedWords[ae]))) {
                askedEntity = true; break;
            }
        }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        var top = null, second = null;
        for (var e = 0; e < ix.entries.length; e++) {
            var rec = ix.entries[e];
            var bestForEntry = null;
            for (var k = 0; k < rec.keyWords.length; k++) {
                var sc = scoreKey(qw, qIdf, qTotal, rec.keyWords[k], ix, mustHit);
                if (!sc) continue;
                if (!bestForEntry || sc.score > bestForEntry.score) bestForEntry = sc;
            }
            if (!bestForEntry) continue;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

            // ⚠️⚠️ **כששואלים על מישהו מהכוורת — התשובה חייבת להיות עליו.**
            //
            // נמדד: "הזכר עובד" קיבל את התשובה "אני עובד בגאווה
            // בעולם הדיגיטלי שדניאל יצר בשבילי". המפתח שניצח היה
            // "איפה אתה עובד" — מילת מפתח אחת, "עובד", שבמקרה
            // מופיעה גם בשאלה. המילה **"הזכר"** פשוט נעלמה
            // מהחישוב, ואיתה כל מה שהשאלה באמת ביקשה.
            //
            // הכלל: אם הילד נקב בשם של מישהו מהכוורת — מלכה,
            // פועלת, זכר, כוורת, דבש — אז ערך שלא מדבר על **אף
            // אחד** מבני הכוורת עונה על שאלה מסוג אחר לגמרי, ולא
            // משנה כמה מילים במקרה חפפו.
            //
            // ⚠️ הכלל נוסח בכוונה רחב ולא צר. הדרישה הצרה ("הערך
            // חייב להזכיר את **אותה** ישות") הייתה פוסלת את
            // "כמה שנים המלכה חיה" ➜ "כמה זמן חיה דבורה", שהיא
            // תשובה נכונה לגמרי. די בכך שהערך עוסק בעולם הדבורים.
            // ⚠️⚠️ **אצל צאבי המסנן הזה מושבת — בכוונה ובהסבר.**
            //
            // אצל באזי הוא הכרחי: הכוורת מלאה ישויות שונות, ותשובה
            // על המלכה במקום על הפועלת היא תשובה שגויה.
            //
            // אצל צאבי **כל** המאגר עוסק בצבי ים, ולכן "הערך מזכיר
            // ישות" נכון כמעט תמיד — והמסנן לא מגן, רק פוסל.
            // נמדד: "כמה עמוק הוא צולל?" הושלם על ידי הבינה
            // ל"...צב גלדי", המילה "צב" הפעילה את המסנן, והערך על
            // הצלילה — שמילות המפתח שלו הן "צלילה", "עומק",
            // "כמה עמוק" בלי שם של ישות — נפסל. הילד שאל על עומק
            // וקיבל תשובה על מין אחר.
            //
            // ההגנה האמיתית כאן היא mustHit: ההתאמה חייבת לגעת
            // במילים שהילד עצמו כתב. היא נשארת פעילה.
            if (false && askedEntity && !rec.hasEntity) continue;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

            bestForEntry.entry = rec.entry;
            if (!top || bestForEntry.score > top.score) { second = top; top = bestForEntry; }
            else if (!second || bestForEntry.score > second.score) second = bestForEntry;
        }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        if (!top) return null;
        if (top.score < MIN_SCORE) return null;
        if (top.qCov < MIN_QCOV) return null;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

        // ⚠️⚠️ **שוויון אינו כישלון — וזה תיקון של תכנון שגוי שלי.**
        //
        // בגרסה הראשונה שוויון החזיר null בהנחה ש"אם לא בטוח, שתוק
        // והנח למנוע הישן". המדידה הראתה שזה בדיוק הפוך: בשאלה
        // "האם המלכה יוצאת מהכוורת" **כמה** ערכים מכילים את המילה
        // "מלכה" ולכן קיבלו ציון דומה, השכבה שתקה — והמנוע הישן
        // בחר "שירותים". כלומר ההסתייגות הזהירה שלי היא שהחזירה
        // את התשובה הגרועה.
        //
        // עכשיו מוחזר תמיד המועמד המוביל, עם דגל ambiguous. מי
        // שקורא מחליט: התאמה חד-משמעית מתקבלת מיד, ומעורפלת
        // משמשת רק כדי **לפסול** בחירה גרועה יותר של המנוע הישן.
        var ambiguous = !!(second && second.score * MARGIN > top.score);
        return {
            entry: top.entry, score: top.score, qCov: top.qCov,
            keyCov: top.keyCov, ambiguous: ambiguous,
            runnerUp: second ? second.entry : null
        };
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    /* הציון של ערך מסוים מול שאלה. משמש להשוואה מול בחירת המנוע
       הישן — בלי זה אי אפשר לדעת אם היא טובה או גרועה. */
    function scoreOf(entry, text, brain, lang) {
        lang = lang || 'he';
        if (!entry || !entry.keys) return 0;
        var ix = index(brain, lang);
        var qw = words(text, lang);
        if (!qw.length) return 0;
        var qIdf = [], qTotal = 0;
        for (var i = 0; i < qw.length; i++) {
            var v = idf(qw[i], ix); qIdf.push(v); qTotal += v;
        }
        if (qTotal <= 0) return 0;
        var bestSc = 0;
        for (var k = 0; k < entry.keys.length; k++) {
            var sc = scoreKey(qw, qIdf, qTotal, words(entry.keys[k], lang), ix);
            if (sc && sc.score > bestSc) bestSc = sc.score;
        }
        return bestSc;
    }
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    // ------------------------------------------------------------------
    // ⚠️ פי כמה המועמד המשוקלל חייב להיות טוב יותר כדי לפסול את
    // בחירת המנוע הישן. 1.5 נבחר אחרי מדידה: נמוך מדי והשכבה
    // מתחילה להתערב במקרים שהמנוע הישן פותר נכון, גבוה מדי והיא
    // לא מצילה את המקרים השבורים.
    var OVERRIDE = 1.5;
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    var API = {
        best: best,
        scoreOf: scoreOf,
        OVERRIDE: OVERRIDE,
        words: words,
        sameWord: sameWord,
        bare: bare,
        idfOf: function (w, brain, lang) { return idf(w, index(brain, lang || 'he')); },
        dfOf: function (w, brain, lang) { return index(brain, lang || 'he').df[bare(w)] || 0; },
        _index: index,
        MIN_SCORE: MIN_SCORE, MIN_QCOV: MIN_QCOV, MARGIN: MARGIN,
        owner: OWNER, ownerEn: OWNER_EN, fingerprint: FINGERPRINT
    };
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */

    if (typeof window !== 'undefined') window.CBY_MATCH = API;
    if (typeof module !== 'undefined' && module.exports) module.exports = API;
})();
/* © דניאל אברהם חדאד · CBY-T7R4L2E9 */
