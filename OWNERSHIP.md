# 🔒 הצהרת בעלות · OWNERSHIP

## אפליקציית "צאבי הצב — שומר צבי הים"
### Chubby the Turtle — Sea Turtle Guardian

| | |
|---|---|
| **יוצר ובעלים בלעדי** | דניאל אברהם חדאד |
| **Creator & sole owner** | Daniel Avraham Haddad |
| **טביעת אצבע / Fingerprint** | `CBY-T7R4L2E9` |
| **מזהה יוצר / Creator ID** | `DAH-CBY-2026` |
| **שנת יצירה** | 2026 |

---

## מה מוגן

כל רכיבי היצירה, ובכלל זה:

- **קוד המקור** — כל קובצי `.js`, `.css` ו-`.html` בפרויקט
- **הדמות "צאבי"** — העיצוב התלת-ממדי, הפרופורציות, לוח הצבעים והאנימציה
- **הדמות "נצנוץ"** — כוכב הים שעל השריון
- **מאגר הידע** — 465 ערכים, כולל ניסוח התשובות והמילים המפתח
- **28 השיעורים** ו-**7 המאמרים** — כולל הניסוח, המבנה והבחירה מה להכניס
- **55 שאלות החידון** — כולל ההסברים וכרטיסי הלימוד
- **5 המשחקים** — המכניקות, 40 השלבים, מודלי התלת-ממד
- **הצלילים** — כולם מסונתזים בקוד, אין בהם קובץ שאול
- **הטקסטים בעברית** — כל מילה בממשק

---

## שכבות ההגנה

⚠️ **קודם כול האמת: אי אפשר למנוע העתקה של קוד צד-לקוח.** כל קובץ נשלח
לדפדפן כטקסט קריא. מי שמבטיח "הגנה מוחלטת" מוכר אשליה.

מה שכן אפשר — ומה שנעשה כאן — הוא **להפוך גניבה לקלה להוכחה ולקשה לניקוי**:

| # | שכבה | איפה | מה היא נותנת |
|---|---|---|---|
| 1 | **סימוני זכויות** | בכל קובץ בפרויקט | יותר מ-2,000 סימונים. הסרה מלאה דורשת מעבר ידני על הכול |
| 2 | **בעלות מפוזרת** | `cby-owner.js` | השם מפוצל ומורכב בזמן ריצה — לא ניתן למחיקה בחיפוש-והחלפה |
| 3 | **בדיקת שלמות** | `cby-owner.js` | סימון הבעלות שנמחק מהמסך מוחזר תוך שניות, והניסיון מתועד |
| 4 | **קנרים** | `cby-owner.js` | מחרוזות ייחודיות חסרות משמעות חיצונית. הופעתן אצל אחר = הוכחה |
| 5 | **הצהרה בקונסול** | `cby-owner.js` | שוללת טענת "לא ידעתי שזה מוגן" |
| 6 | **תקציר SHA-256** | `OWNERSHIP.json` | מוכיח שקובץ בתוכן מסוים היה קיים אצלך בתאריך מסוים |
| 7 | **חותמת זמן חיצונית** | היסטוריית Git | ⚠️ **החזקה מכולן** — תאריך שנרשם מחוץ למחשב שלך |

### הקנרים

```
CBY-T7R4L2E9        DAH-CBY-2026        CBY-PROOF
cby_kav_7r4l        chubby_haddad_seed  cby-nitznutz-2026
```

⚠️ `cby_kav_7r4l` נראה כמו מפתח אחסון תפעולי לגמרי — ולכן מי שמעתיק
משאיר אותו במקום, ואין לו שום הסבר לגיטימי למה הוא שם.

---

## כלים

```
node tools/mark.js            סימון בעלות בכל הקבצים (+ ספירה)
node tools/mark.js --count    ספירה בלבד
node tools/fingerprint.js     יצירת OWNERSHIP.json עם SHA-256
node tools/cache-version.js --fix   העלאת גרסת המטמון
node tools/build-dist.js      בניית תיקיית הפרסום
publish.bat                   פרסום מלא לאותו קישור
```

---

## בדיקה מהירה

בקונסול של הדפדפן, באפליקציה החיה:

```js
CBY_OWNER.certificate()   // תעודת בעלות מלאה
CBY_OWNER.verify()        // בדיקת שלמות הסימונים
CBY_OWNER.tamperLog()     // ניסיונות הסרה שתועדו
```

---

## מה עוד כדאי לעשות

⚠️ שום שורת קוד לא מחליפה **רישום מסודר**:

1. **לדחוף ל-GitHub** אחרי כל שינוי משמעותי — חותמת זמן חיצונית.
2. **לשלוח לעצמך במייל** את `OWNERSHIP.json` מדי פעם — חותמת זמן נוספת אצל צד שלישי.
3. **לשמור גיבוי** של הגרסאות המרכזיות.
4. בישראל אין חובת רישום זכות יוצרים — היא קמה מעצם היצירה. מה שנדרש
   בבית משפט הוא **הוכחת מועד ותוכן**, וזה בדיוק מה שהמנגנון כאן מייצר.

---

```
© 2026 דניאל אברהם חדאד · Daniel Avraham Haddad
כל הזכויות שמורות · ALL RIGHTS RESERVED
CBY-T7R4L2E9 · DAH-CBY-2026
```
